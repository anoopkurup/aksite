import { Card, CardTitle, CardBody } from "@anoopkurup/design-system";

// CardTitle is the serif heading slot of a Card — shown in context.
export const InCard = () => (
  <Card className="max-w-md">
    <CardTitle>The CLEAR engagement</CardTitle>
    <CardBody>
      One packaged offer, proven against real prospects, with a weekly motion
      that produces conversations.
    </CardBody>
  </Card>
);
